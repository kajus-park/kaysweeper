# Plan

increasing difficulty + high score?
settings (vibration, difficulty, colorscheme?)
abort long press properly

## done

Draw numbers using pixels using rects
store numbers as 8x8 as 8 bytes
playing/won/lost state  Translucent text
haptic feedback on flag
add a counter for the number of bombs
draw_bomb(x,y,size)
draw_number(x,y,size,number,color) // reuse for bombs remaining? and timer?
seed rng
